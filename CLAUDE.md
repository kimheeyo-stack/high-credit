# CLAUDE.md — 하이학점 (High-Credit)

이 파일은 Claude Code가 이 프로젝트에서 작업할 때 항상 먼저 읽는 "프로젝트 설명서"입니다.
새 코드를 만들거나 고치기 전에, 아래 규칙과 도메인 지식을 반드시 지켜 주세요.

---

## 1. 프로젝트 한 줄 요약

> "내가 원하는 진로에 맞춰 복잡한 고교학점제를 알아서 계산하고 관리해 주는 모바일 비서"

- 서비스명(가칭): 하이학점 (High-Credit)
- 형태: iOS + 안드로이드 모바일 앱
- 사용자 2종: (1) 고등학생 (2) 고등학생 자녀를 둔 학부모

---

## 2. 기술 스택 (이 스택을 벗어나지 말 것)

| 영역 | 사용 도구 | 비고 |
| --- | --- | --- |
| 앱 화면 | Flutter (Dart) | iOS·안드로이드 동시 빌드 |
| 백엔드 | Supabase | 인증, DB(PostgreSQL), 파일저장, 알림 |
| 외부 공개데이터 | 나이스 오픈 API (open.neis.go.kr) | 학사일정·학교정보만 |
| 성적 입력 | 수동 입력 (MVP) → OCR (후순위) | 개인 성적은 자동 수집 불가 |
| 개발 보조 | Claude Code | |
| 화면 설계 | Figma | |

> 새 라이브러리나 다른 프레임워크를 도입하기 전에 반드시 이유를 설명하고 확인받을 것.

---

## 3. 절대 규칙 (어기면 안 됨)

1. **나이스에서 개인 성적/출결/이수학점을 자동 수집하지 않는다.**
   - 나이스 오픈 API에는 개인 데이터가 없으며, 로그인 스크래핑은 약관 위반 + 미성년자 민감정보 처리로 법적 위험이 크다.
   - 개인 데이터는 "사용자 본인이 직접 입력"하거나 "본인이 촬영한 성적표 OCR"로만 받는다.
2. **미성년자 데이터 보호를 최우선으로 한다.**
   - 학부모의 자녀 모니터링은 반드시 "자녀 동의 → 연동 코드 발급 → 자녀가 언제든 해제 가능" 구조.
   - 동의 없이 학부모가 자녀 데이터를 보게 만드는 코드는 작성하지 않는다.
3. **민감정보(성적 등)는 URL/쿼리스트링에 절대 싣지 않는다.** 본문(body)으로만 전송.
4. **결제/계정삭제/권한변경 등 되돌릴 수 없는 동작은 반드시 사용자 확인 단계를 둔다.**

---

## 4. 작업 방식 (워크플로 — 반드시 이 순서로 진행)

모든 작업은 아래 흐름을 지킨다. 한 번에 몰아서 하지 말고 **한 단계씩 차례대로** 진행한다.

### 4-1. 순차 진행 원칙
- `plan.md`의 **미완료 항목 중 가장 위(다음 순서)** 부터 하나씩 처리한다.
- 임의로 단계를 건너뛰거나 여러 단계를 동시에 진행하지 않는다.
- 한 항목을 끝내고 검토·승인까지 마친 다음에야 그 다음 항목으로 넘어간다.
- 진행 중 막히거나 결정이 필요하면 멈추고 사용자에게 물어본다.

### 4-2. 개발팀 / 검토팀 분리 (둘 다 Claude가 역할을 나눠 수행)
1. **[개발팀]** 해당 단계의 기능을 구현한다.
2. **[검토팀]** 구현이 끝나면 코드를 처음부터 검토한다.
   - 버그·오류, 동작 안 하는 부분
   - 이 문서(CLAUDE.md)의 절대 규칙 위반 여부 (특히 미성년자 데이터, 나이스 개인정보 자동수집 금지)
   - 고교학점제 계산 규칙(192학점, 성취율 40% 미이수 등) 정확성
3. **오류가 있으면** → 검토팀이 문제를 정리해 알려주고, 개발팀이 수정 → 다시 2번 검토 (오류가 없어질 때까지 반복).
4. **오류가 없으면** → 검토팀이 "검토 완료, 오류 없음"을 보고하고,
   **사용자에게 "빌드해도 될까요?"라고 물어본 뒤, 승인을 받고 나서만 빌드한다.**
   사용자 승인 없이 임의로 빌드하지 않는다.

### 4-3. plan.md 갱신 (매 작업·수정·보완마다)
- 작업을 하거나 수정·보완할 때마다 **그때그때 `plan.md`를 업데이트**한다.
- 완료한 항목은 체크박스를 `[x]`로 바꿔 표시한다 (미완료는 `[ ]` 유지).
- `plan.md` 맨 위의 "현재 상태"에 **마지막 작업 내용·날짜·다음 할 일**을 적는다.
- "작업 로그"에 무엇을 했는지 한 줄씩 기록한다.
- 목적: **언제 재접속하든 `plan.md`만 보면 어디까지 진행됐는지 바로 알 수 있게** 한다.

---

## 5. 핵심 도메인 지식 — 고교학점제 (2025학년도 전면 적용)

계산 로직을 만들 때 이 규칙을 정확히 반영할 것.

- **졸업 기준: 누적 192학점 이상.**
- **과목 이수 조건 (둘 다 충족해야 학점 취득):**
  - 과목 출석률: 수업 횟수의 2/3 이상
  - 학업 성취율: 40% 이상 (지필+수행 100점 만점 기준 40점 이상)
- **성취도 등급:** 성취율에 따라 A~E.
  - 성취율 40% 미만 → 성취도 **I (미이수)** 로 처리.
  - 미이수 시 보충이수 과정을 거쳐야 하며, 못 채우면 졸업 유예 가능.
- **최소 신청 학점:** 학기당 최소 28학점 신청(조기졸업 불가).
- **평가 방식:** 공통과목은 석차등급 병기, 선택과목(일반·융합·진로)은 성취평가제(A~E).
- 적용 대상 확대: '25년 1학년 전과목 → '26년 1·2학년 → '27년 전학년.

> 제도는 자주 바뀐다. 계산 규칙은 코드에 흩어 놓지 말고 `lib/domain/credit_rules.dart` 같은 한 곳(설정값/상수)에 모아 두어 정책 변경 시 한 군데만 고치도록 설계할 것.

---

## 6. 데이터 모델 (초안)

- `users` — id, 역할(student/parent), 학교코드, 학년, 진로계열
- `subjects` — 과목 마스터(과목명, 교과영역, 학점, 공통/선택 구분)
- `enrollments` — 사용자별 수강 과목(학기, 과목, 학점, 출석률)
- `scores` — enrollment별 지필/수행 점수 → 성취율·성취도(A~E/I) 계산
- `links` — 학부모-자녀 연동(연동코드, 동의여부, 활성상태)
- `events` — D-Day(수행평가/고사/수강신청), 마감일
- `career_roadmaps` — 진로계열별 추천 과목 묶음

---

## 7. 디렉토리 구조 (제안)

```
lib/
  main.dart
  domain/        # 학점 계산 규칙, 핵심 로직 (UI/DB와 분리)
    credit_rules.dart
  data/          # Supabase 연동, 모델
  features/
    dashboard/
    simulator/   # 학점 시뮬레이터 (MVP 핵심)
    timetable/
    career/
    parent/
  shared/        # 공통 위젯, 테마
```

---

## 8. 코딩 컨벤션

- 한국어 UI 문구는 한 곳(`l10n` 또는 상수 파일)에 모은다.
- 도메인 로직(학점 계산)은 UI/네트워크와 분리하고, 반드시 단위 테스트를 붙인다.
- 비전문가 사용자가 읽을 에러 메시지는 친절한 한국어로.
- 커밋 전 `flutter analyze` 통과.

---

## 9. 지금 우선순위 (MVP)

학생용 **"학점 시뮬레이터 + 미이수 경고"** 단일 기능이 최우선.
성적 수동 입력 → 학점 게이지 시각화 → 졸업요건 진단 → 미이수(성취율 40% 미만) 경고.
진로 추천·시간표·학부모 기능은 이후 단계.
**작업 순서와 현재 진행상황은 항상 `plan.md`를 기준으로 한다** (4-3 규칙대로 매번 갱신).
